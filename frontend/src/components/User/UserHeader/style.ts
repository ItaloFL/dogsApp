import styled from "styled-components";

export const UserHeaderWrapper = styled.header`
  display: grid;
  grid-template-columns: 1fr auto;
  align-items: center;
  margin-bottom: 2rem;
  position: relative;
`
